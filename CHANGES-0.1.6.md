# 0.1.6 修改说明

## 手机使用情况

- 新增 Android `PACKAGE_USAGE_STATS` 特殊访问权限和系统设置入口。
- 设备页可查看今日可见前台使用时长、娱乐类时长和使用较多的 App。
- 使用 `UsageEvents` 按学习时段在本地计算前台时长，而不是把全天数据直接当作学习时段。
- 游戏、视频、社交类 App 和常见短视频包名会被标记为娱乐类。

## 学习计划识别

- 识别“我去学习”“十分钟后复习一小时”“我去图书馆学习了”等表达。
- 支持分钟、小时、半小时及常见中文数字。未说时长时默认 60 分钟。
- 学习计划保存在本机，App 重启后仍可继续复核。
- 说“不学了”会取消计划；说“学完了”会进行最后一次本地复核后清除。

## 抓包规则

- 计划开始至少 5 分钟后，你再次发消息时才会抽查。
- 两次本地抽查至少间隔 15 分钟，每个计划最多 4 次。
- 娱乐类使用时长达到 `max(3分钟, 已过时间的20%)` 时，才把摘要交给模型。
- 娱乐时长达到 `max(8分钟, 已过时间的33%)` 时标记为较强证据，允许林清野更直接地抓包。
- 只有触发阈值时才向 API 增加精简摘要，不额外调用 API。

## 构建配置

- Gradle Wrapper 从 9.3.0 调整为 8.9，以匹配项目使用的 Android Gradle Plugin 8.7.3。

## 版本

- versionCode: 7
- versionName: 0.1.6
