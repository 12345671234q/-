# 0.1.7 主动消息与后台抓包

- 新增 WorkManager 后台任务：学习计划与行程计划创建时，本地安排延时复核。
- 学习计划：30 分钟及以上会安排一次中途抽查，结束时再验收一次；娱乐/游戏/短视频/社交使用达到阈值时才调用 API 生成抓包消息。
- 行程计划：约 12 分钟和 30 分钟后复核位置；定位精度正常且仍接近出发点时才调用 API。
- 新增主动消息预算：默认每天最多 8 条，最短间隔 15 分钟；可在“设定 → 主动消息”调整。
- 不连续轰炸：一条主动消息发出后，在用户回复前不再发送下一条。
- 新增 Android 系统消息通知：高优先级消息渠道、横幅/通知中心显示，点击直接返回聊天。
- 修正通知点击行为：即使 App 当时停留在“设备”或“设定”页，点击主动消息也会切回聊天页。
- Android 13 及以上新增通知运行时权限入口。
- 行程后台复核新增“始终允许位置”状态提示；学习抓包不需要后台位置权限。
- 主动消息会写入本地聊天记录；App 在前台时通过本地广播即时刷新，后台时点击通知后同步。
- API 不可用时使用本地短句兜底，避免抓包完全失效。
- 新增测试通知按钮。
- 版本号升级为 0.1.7（versionCode 8）。
