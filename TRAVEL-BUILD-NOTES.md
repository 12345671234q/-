# 寂墨·旅途版 0.3.0

这是基于 0.1.10 源码制作的独立临时版本，包名为 `com.privatecompanion.chat.travel`，可以和正式版同时安装。

## 记忆规则

- 自动记忆先进入“待确认”隔离区，未确认前不会加入模型请求。
- 每条记忆区分得知时间与实际发生时间，并保存用户原话和消息 ID。
- 助手回复、推测、角色扮演、小说和假设不能成为事实来源。
- 固定只代表防止清理，不代表每轮强制注入。
- 自动摘要作为待审核备份保存，不直接作为历史事实注入。
- 涉及过去事实的回复自动降低 temperature。
- 短期情绪使用模糊隶属度混合并随时间衰减，绝不作为事实证据。

## 回到正式版前

进入“设定 → 导出临时版数据”，保存 JSON 文件。导出内容包括聊天、结构化记忆、证据和情绪状态，不包含 API Key。

## 注意

本版本不包含 0.2.0 后来加入的锁机与拦截页面。位置、学习检查和主动通知仍来自 0.1.10 源码能力。
